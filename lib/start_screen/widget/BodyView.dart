import 'package:flutter/material.dart';

class BodyViewS extends StatelessWidget {
  const BodyViewS({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}